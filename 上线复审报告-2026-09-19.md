# 清账智能财务系统 · 上线复审报告

- 复审日期：2026-09-19
- 复审对象：清账智能财务系统（Spring Boot 2.7 + Vue3/Element Plus，部署于生产服务器）
- 关联文档：《代码审计报告-2026-09-05》《清账智能财务系统-完整操作手册-V2》
- 结论：**通过复审，具备上线条件**（须先完成文末"上线前必办事项"）

## 一、复审范围
覆盖代码安全、业务正确性、数据库变更、部署配置与线上运行状态五个方面，并对前次审计发现问题逐项复核。

## 二、前次审计问题整改复核（已验证）
| 问题 | 整改 | 复核结果 |
| --- | --- | --- |
| 用户新增/编辑带角色报错（ClassCastException） | 角色 ID 安全解析 toLongList() | 已修复，回归通过 |
| 调试接口 /debug/encode 暴露 | 已移除 | 线上返回 404 |
| 接口文档/Druid 监控对外暴露 | 生产关闭并门控 | 未授权访问返回 401 |
| 可删除自身/内置管理员/最后管理员 | 三重删除保护 | 已修复，回归通过 |
| 已审核/已过账凭证可删除 | 删除拦截非草稿状态 | 已修复，回归通过 |
| 依赖组件已知漏洞 | hutool 5.8.32 / fastjson 2.0.53 | 已升级 |
| 敏感配置硬编码 | 改为环境变量注入，外置配置权限 600 | 已修复 |
| 业务单据无法自动转凭证 | 新增 BizVoucherService 五类单据 | 已实现，回归通过 |
| 费用未付款科目口径不清 | 未付挂其他应付款(2241)，付款冲销 | 已实现，回归通过 |
| 数据库缺关联字段/索引 | sql/26-audit-fixes.sql 幂等脚本 | 已应用 |

## 三、线上安全抽查（本轮实测）
- 首页 / 登录：200，登录正常。
- 凭证列表 23 条、销售发票 9 条，接口数据正常。
- `/api/debug/encode` → 404（已移除）。
- `/api/druid/index.html` → 404（Druid 已关闭）。
- `/api/doc.html`、`/api/swagger-resources`、`/api/v3/api-docs` 未授权 → 401（已门控）；生产 `knife4j.production=true`。
- 全库扫描：代码与文档中不含服务器/数据库口令等敏感信息。

## 四、业务回归测试
自动化回归 88/88 项断言全部通过，覆盖：五类业务凭证生成（科目正确、借贷平衡）、制单与审核职责分离、凭证审核/过账/反过账生命周期、已过账凭证禁删、核销清零、应收应付一致性、期间结账拦截未过账凭证并记录结账人、试算平衡（借=贷）、用户删除保护、未授权/越权/调试接口拦截。

## 五、已识别并接受的风险（建议后续优化）
1. JWT 黑名单存于内存，多实例不共享——当前单实例可控，扩容时改用 Redis。
2. 单据编号基于时间戳，极端并发可能重复——业务量下概率极低，可加唯一约束。
3. 税额按简化口径计算——满足当前业务，对接税控系统时细化。
4. Spring Boot 2.7 进入维护末期——建议规划升级 3.x 长期支持版。
5. 默认管理员口令为弱口令——见下方必办事项。

## 六、上线前必办事项
1. **立即修改默认管理员口令 admin/123456**，启用强密码与定期更换策略。
2. 确认数据库口令、JWT 密钥仅存于服务器环境变量，未入库/入代码库。
3. 按角色复核授权，落实制单与审核职责分离；配置各业务审批人与金额阈值。
4. 核对系统参数科目映射与实际会计科目一致；导入期初数据并试算平衡。
5. 演练一次完整月结（录入→审核→过账→结账→报表）；配置数据库定时备份并验证可恢复。
6. 建议 Nginx 启用 HTTPS 与访问日志。

> 说明：`release/finance-app.jar` 约 66.5MB，超过 GitHub 建议的 50MB 单文件上限（未超 100MB 硬限制），如需瘦身可迁移 Git LFS。
