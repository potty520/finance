# 清账（QingZhang）财务系统代码审计报告

- 审计日期: 2026-09-05
- 审计范围: finance-app（Spring Boot 2.7.18 后端）、finance-web（Vue3 前端）、sql 脚本、deploy.sh
- 代码版本: master @ 6b73129
- 结论: **不建议以当前状态直接上线**。存在 3 个会计引擎级缺陷、多项安全加固缺口。功能测试（69/69 接口、30/30 业务用例）通过只能说明"流程能走通"，不能说明"账务是正确的"。

---

## 一、P0 严重 — 会计引擎缺陷（必须修复）

### 1. 总账余额表（gl_balance）从未被写入
- 全局搜索确认：`gl_balance` 只出现在 SELECT 中（GlBalanceMapper.xml、ReportMapper.xml），没有任何 INSERT/UPDATE。
- `GlVoucherServiceImpl.post()` 只把凭证状态改为 POSTED，不更新任何科目余额。
- 后果：资产负债表、利润表、科目余额表全部读 `gl_balance`，**报表数据与实际凭证完全脱节**（除非手工导 SQL）。这是财务系统最核心的缺陷。
- 修复方向：过账时按分录聚合写入/更新 `gl_balance`（期间发生额 + 期末余额），反过账时回冲；用事务保证一致性。

### 2. 期末结账无余额结转
- `GlPeriodController.close()` 只把期间状态改为 CLOSED，硬编码操作人为 1L"系统用户"。
- 没有生成下期期初余额的逻辑，也没有损益结转（损益类科目期末应结转本年利润）。
- 后果：即使补上第 1 条，跨期报表的期初数仍是错的。

### 3. 业务单据与总账零联动
- 应收发票/收款、应付发票/付款、费用报销、出纳、资产折旧、存货等业务模块均不生成凭证（全局搜索 `new GlVoucher(` 仅出现在总账模块自身的冲销逻辑）。
- 后果："智能财务系统"的业财一体不存在，所有凭证必须手工录入，业务数据与账务必然不一致。
- 修复方向：按单据类型实现凭证模板（如 销售发票 → 借 应收账款 / 贷 主营业务收入 + 应交增值税-销项）。

---

## 二、P1 高 — 安全问题

### 4. 凭据入库 + 发布包含密钥的 jar
- `finance-app/src/main/resources/application.yml` 硬编码了外网开发库密码（103.236.96.82:13306）与 JWT 密钥 `finance-app-jwt-secret-key-2026-please-replace-in-production`，自提交 d05dd6f 起永久留在 git 历史。
- `release/finance-app.jar`（72MB，含上述配置）与 `release/finance-web-dist.zip` 已提交到仓库。任何有仓库读权限的人都能解包拿到生产配置。
- 修复：凭据改为环境变量/外部配置；从仓库移除 jar 与 zip（改用 Release/制品库）；**立即轮换**开发库密码与 JWT 密钥（密钥一旦泄露，旧 token 均可伪造，需 git 历史清理 + 换密钥）。

### 5. 生产弱口令
- 生产 admin/123456 仍在使用；服务器 JWT 密钥为可猜测格式。登录无失败锁定、无验证码、无限流（/auth/captcha 在白名单但后端没有实现该端点）。
- 修复：强制修改管理员密码、加强口令策略（changePwd/resetPwd 均无复杂度校验）、实现登录限流。

### 6. 凭证模块权限设计失效（职责分离缺失）
- `GlVoucherController` 所有写操作均为 `hasAnyAuthority('gl:voucher:xxx', 'gl:voucher:list')` —— 只读权限 `gl:voucher:list` 成了万能通行证：能看凭证列表的人即可新增、修改、删除、审核、过账、反过账、冲销。
- `approve()` 允许同一个人制单→提交→审核一条龙；`audit()` 不校验审核人 ≠ 制单人；过账也不校验过账人 ≠ 审核人。
- 财务系统最基本的内控（制单/审核/过账三分离）完全缺失。
- 修复：去掉所有 `@PreAuthorize` 中的 `gl:voucher:list` 兜底；audit/post 校验操作人与制单人不同（可配置开关）。

### 7. 服务以 root 运行
- `deploy.sh` 生成的 systemd 单元 `User=root`。Java 进程被攻破即等于服务器沦陷。
- 修复：创建专用用户（如 qingzhang），目录授权给该用户。

### 8. API 文档匿名暴露
- `/doc.html`、`/v2/api-docs/**`、`/swagger-resources/**` 在 SecurityConfig 白名单中且 `knife4j.enable: true`，生产环境任何人可浏览全部接口结构（含参数）。
- 修复：生产关闭 knife4j（spring.profiles 区分），或移出白名单加认证。

### 9. 用户角色保存是空实现
- `SysUserServiceImpl.saveUserRoles()` 与 `updateUser()` 中角色保存均为空方法（注释"简化"）。通过界面给用户分配角色**不会生效**，`sys_user_role` 表不会被写入（新增用户时）。
- 后果：权限体系实际只靠预置数据支撑；这是功能性 bug，也影响权限审计。

---

## 三、P2 中 — 业务逻辑与加固

### 10. 审批流形同虚设
- 审批人硬编码为 `userId=2`（`WorkflowServiceImpl.start()` 中 `setApproverId(2L)/setAssignee(2L)`），名称"审批员i"；schema 里的 `wf_approval_config` 配置表完全没用上。
- 审批通过后**不回写业务单据状态**：费用申请批完后 `exp_application.status` 永远是 0。驳回同样不回写。
- 审批实例"进行中"和"已完成"用同一个 status=1 表示，无法区分。

### 11. JWT 黑名单仅内存
- `JwtUtil.blacklist` 是 ConcurrentHashMap：重启即失效、多实例不共享、无定时清理调度（`cleanExpiredBlacklist` 无人调用，黑名单无限增长）。

### 12. CORS `*` + allowCredentials
- `addAllowedOriginPattern("*")` 与 `setAllowCredentials(true)` 同时启用。虽然当前认证走 Bearer 头，仍应收敛为白名单域名。

### 13. 客户端 IP 可伪造
- `getClientIp()` 无条件信任 `X-Forwarded-For`，登录日志中的 IP 可被任意伪造。应只信任来自已知反代的头，或固定取 nginx 传入的 X-Real-IP。

### 14. 金额校验缺口
- `ExpenseServiceImpl.repayLoan()`：只校验 `amount <= 待还余额`，**不校验 amount > 0**。传负数可使已还金额减少、待还金额增加（数据破坏型漏洞）。
- `ArServiceImpl.saveInvoice()` 等不校验金额非负。
- 建议所有金额入口统一校验（正数、上限、小数位）。

### 15. 审计痕迹破坏
- 应收核销 `w.setOperator(1L); setOperatorName("系统用户")` 硬编码；期末结账同样硬编码 1L。真实操作人丢失，追责/审计不可能。应从 SecurityContext 取当前用户。

### 16. 单号生成全部基于时间戳
- 14 处用 `System.currentTimeMillis()` 生成单号（AR/AP/费用/合同/资产/存货/工作流…）；凭证号用 `count+1`。并发下会撞号，删除后凭证号复用。
- 修复：数据库唯一约束 + 序号表/发号器，或至少 时间戳+随机后缀。

### 17. 核销并发与状态校验
- `writeoff()` 不校验发票/收款单是否为已审核状态（草稿可被核销）；先读后写无乐观锁，并发核销可超额核销。

### 18. 税务模块简化过度
- 税率硬编码 13%；不含税销售额 = 销项税 ÷ 0.13 倒推（混合税率企业必错）；进项转出恒为 0；上期留抵只回溯上一期（上上期断档则留抵丢失）。至少应把税率做成科目/单据级配置，并在界面标注"简化口径"。

### 19. 依赖版本
- Spring Boot 2.7.18 已 EOL；内含 Spring Framework 5.3.31 受 CVE-2024-22243/22259/22262（UriComponentsBuilder）影响，建议升 2.7 线最后补丁或迁移 3.x。
- fastjson2 2.0.43（建议 ≥2.0.52）、hutool 5.8.25（建议 ≥5.8.32）。
- 生产包里带 H2（runtime），无必要。

### 20. 生产日志级别
- application.yml 开 `com.finance: debug` + MyBatis StdOutImpl，SQL 与参数全量打印到日志（数据泄露面 + 性能）。生产 profile 应覆盖为 info 且关闭 SQL 打印（deploy.sh 生成的 prod 配置已覆盖为 info，这点是好的，但仓库默认配置仍危险）。

### 21. 其他
- `/debug/encode` 匿名开放（BCrypt 单向，风险低，但生产不应保留调试端点）。
- `${ew.sqlSegment}`（GlVoucherMapper.xml）：MyBatis-Plus wrapper 标准用法，当前调用方均为内部构造，暂无外部注入路径，保留关注。
- 认证/授权失败统一返回 HTTP 200（body 里 code=401/403）：监控、WAF、浏览器均无法识别，建议恢复标准状态码。

---

## 四、P3 低 — 质量项

1. 前端路由守卫只判 token 存在，未按菜单/权限过滤（后端有兜底，但界面可越权展示）。
2. Token 存 localStorage：常规做法，当前无 v-html 注入点，风险可控；如后续引入富文本渲染需警惕。
3. 用户删除只禁止 id=1，未禁止删除/禁用自己及最后一个管理员。
4. schema 只有主键：`period_code`、`status`、`subject_code`、`voucher_id`、`bill_no`、`customer_id` 等高频列全无二级索引，数据量上来后全表扫描；无外键约束。
5. deploy.sh 中 `mysql -p"$DB_PASS"` 密码进进程列表；`DB_PASS` 默认值 `change-me-please` 若忘改会直接用。
6. 无单元测试（`src/test` 为空），回归全靠手写脚本。

---

## 五、做得好的地方

- 密码 BCrypt 哈希，登录错误信息不区分用户不存在/密码错误。
- 文件上传：扩展名白名单 + `..` 拦截 + 归一化后校验路径不逃逸 + UUID 重命名；下载同样校验。
- 全局异常处理不泄露堆栈，统一返回"系统繁忙"。
- 系统用户/角色模块有方法级 `@PreAuthorize`；URL 级 antMatchers 覆盖了全部模块的写操作。
- 所有 SQL 均为参数化（#{} 或 ?），未发现 SQL 注入点。
- 分页大小有上限（MAX_PAGE_SIZE）。
- 前端无 v-html，XSS 面小。

---

## 六、上线前最小修复清单（按优先级）

1. 实现过账写 `gl_balance` + 反过账回冲（含事务）——否则报表不可用。
2. 修 `saveUserRoles` 空实现（角色分配不生效）。
3. 凭证权限去掉 `gl:voucher:list` 兜底；加制单≠审核校验。
4. `repayLoan` 等补金额为正数校验。
5. 核销/结账/审批改从 SecurityContext 取真实操作人。
6. 移除/轮换 application.yml 中的凭据；仓库移除 release/*.jar；轮换生产 JWT 密钥与 DB 密码；改 admin 密码。
7. systemd 改专用用户；生产关 knife4j；CORS 收敛。
8. 审批流接入配置表 + 审批结果回写业务单据（否则审批功能名存实亡）。
9. 补关键列索引。
10. 升级 Spring Framework 至 5.3.39（2.7 线最后补丁）或规划 3.x 迁移。

业务-凭证联动（第 3 条 P0）与期末结转可作为第二阶段，但上线宣传口径上不应声称"自动生成凭证/自动报表"。
