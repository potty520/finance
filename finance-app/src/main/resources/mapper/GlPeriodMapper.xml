<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.finance.module.ledger.mapper.GlPeriodMapper">

    <select id="selectByYearPeriod" resultType="com.finance.module.ledger.entity.GlPeriod">
        SELECT * FROM gl_period
        WHERE fiscal_year = #{year} AND period_index = #{period} AND deleted = 0
        LIMIT 1
    </select>

    <select id="selectCurrent" resultType="com.finance.module.ledger.entity.GlPeriod">
        SELECT * FROM gl_period
        WHERE deleted = 0
        ORDER BY fiscal_year DESC, period_index DESC
        LIMIT 1
    </select>

</mapper>
