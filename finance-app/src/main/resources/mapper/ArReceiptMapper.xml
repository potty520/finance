<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.finance.module.receivable.mapper.ArReceiptMapper">

    <select id="sumUnapplied" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(unapplied_amount), 0)
        FROM ar_receipt
        WHERE customer_id = #{customerId} AND deleted = 0
    </select>

    <select id="selectByCustomer" resultType="com.finance.module.receivable.entity.ArReceipt">
        SELECT * FROM ar_receipt
        WHERE customer_id = #{customerId} AND deleted = 0
        ORDER BY create_time DESC
    </select>

</mapper>
