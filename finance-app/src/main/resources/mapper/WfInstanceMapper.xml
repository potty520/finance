<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.finance.module.workflow.mapper.WfInstanceMapper">

    <select id="selectByBusiness" resultType="com.finance.module.workflow.entity.WfInstance">
        SELECT * FROM wf_approval_config
        WHERE business_type = #{businessType} AND business_id = #{businessId} AND deleted = 0
        ORDER BY id DESC
    </select>

</mapper>
